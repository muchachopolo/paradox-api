var expect = require('chai').expect;
var path = require('path');

var areaService = require('../../main/api/services/area-service');


beforeEach((done)=> {
    done();
});

describe('areaService', function () {
    describe('#test', function () {
        it('it works !', function (done) {
            done();
        });
    });
});